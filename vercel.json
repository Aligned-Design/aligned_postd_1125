{
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...all]"
    }
  ],
  "functions": {
    "api/[...all].ts": {
      "maxDuration": 60,
      "includeFiles": "dist/server/**"
    }
  },
  "_comment_crons": "⚠️ CRON CONFIGURATION REQUIRED IN VERCEL UI - see deployment docs",
  "_comment_why": "Vercel does not interpolate env vars in vercel.json. Configure cron manually in Vercel Dashboard → Crons with: Path=/api/crawl/process-jobs?secret=<actual-value> Schedule=every-minute"
}
